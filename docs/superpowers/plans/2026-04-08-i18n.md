# i18n (다국어 지원) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Korean/English i18n support to the UI Kit demo site using react-i18next with URL-based locale routing (`/:locale/components/...`).

**Architecture:** URL의 `:locale` 파라미터로 언어를 결정하고, `LocaleLayout` 래퍼가 `i18n.changeLanguage()`를 호출한다. 번역 파일은 namespace별로 분리하며 (common, autocomplete), 2개 언어이므로 번들에 직접 포함한다. 기존 Autocomplete 컴포넌트의 하드코딩된 한국어 기본값은 `useTranslation`으로 교체하되, props 전달 시 props가 우선한다.

**Tech Stack:** i18next, react-i18next, React Router v7 (기존)

---

## File Structure

```
apps/ui-kit/
├── src/
│   ├── i18n.ts                              (Create) — i18next 초기화
│   ├── main.tsx                             (Modify) — i18n import 추가
│   ├── App.tsx                              (Modify) — 라우트 구조 변경
│   ├── locales/
│   │   ├── ko/
│   │   │   ├── common.json                  (Create) — 공통 번역 키
│   │   │   └── autocomplete.json            (Create) — autocomplete 번역 키
│   │   └── en/
│   │       ├── common.json                  (Create) — 공통 번역 키
│   │       └── autocomplete.json            (Create) — autocomplete 번역 키
│   ├── layouts/
│   │   ├── LocaleLayout.tsx                 (Create) — locale 파라미터 → i18n 동기화
│   │   ├── DocsLayout.tsx                   (Modify) — locale-aware NavLink + 언어 전환 UI
│   │   └── DocsLayout.module.css            (Modify) — 언어 전환 UI 스타일
│   ├── components/autocomplete/
│   │   ├── Autocomplete.tsx                 (Modify) — 기본 텍스트 i18n 전환
│   │   └── Autocomplete.test.tsx            (Modify) — i18n setup 추가
│   ├── pages/autocomplete/
│   │   └── AutocompletePage.tsx             (Modify) — 모든 텍스트 t() 호출로 교체
│   └── setup-tests.ts                       (Modify) — 테스트용 i18n mock 설정
├── e2e/
│   └── autocomplete.spec.ts                 (Modify) — locale URL + 양쪽 locale 테스트
└── package.json                             (Modify) — i18next, react-i18next 의존성 추가
```

---

### Task 1: Install Dependencies

**Files:**
- Modify: `apps/ui-kit/package.json`

- [ ] **Step 1: Install i18next and react-i18next**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm add i18next react-i18next
```

- [ ] **Step 2: Verify installation**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm ls i18next react-i18next
```

Expected: Both packages listed with versions.

- [ ] **Step 3: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/package.json apps/ui-kit/pnpm-lock.yaml pnpm-lock.yaml && git commit -m "chore: add i18next and react-i18next dependencies"
```

---

### Task 2: Create Translation JSON Files

**Files:**
- Create: `apps/ui-kit/src/locales/ko/common.json`
- Create: `apps/ui-kit/src/locales/ko/autocomplete.json`
- Create: `apps/ui-kit/src/locales/en/common.json`
- Create: `apps/ui-kit/src/locales/en/autocomplete.json`

- [ ] **Step 1: Create `src/locales/ko/common.json`**

```json
{
  "siteTitle": "UI Kit",
  "loading": "로딩 중...",
  "language": {
    "ko": "한국어",
    "en": "English"
  }
}
```

- [ ] **Step 2: Create `src/locales/en/common.json`**

```json
{
  "siteTitle": "UI Kit",
  "loading": "Loading...",
  "language": {
    "ko": "한국어",
    "en": "English"
  }
}
```

- [ ] **Step 3: Create `src/locales/ko/autocomplete.json`**

```json
{
  "component": {
    "noResults": "결과 없음",
    "loading": "로딩 중...",
    "resultCount_one": "{{count}}개의 결과",
    "resultCount_other": "{{count}}개의 결과"
  },
  "page": {
    "title": "Autocomplete",
    "description": "사용자가 입력하면 항목을 필터링하여 제안하는 combobox 컴포넌트입니다. 키보드 내비게이션, 커스텀 렌더링, 비동기 데이터 로딩을 지원합니다.",
    "demo": {
      "sectionTitle": "라이브 데모",
      "basic": "기본 — 로컬 필터링",
      "async": "비동기 — API 호출 시뮬레이션 (500ms 딜레이)",
      "custom": "커스텀 렌더링 — 국기가 있는 국가 목록",
      "basicPlaceholder": "과일 검색...",
      "asyncPlaceholder": "과일 검색 (비동기)...",
      "customPlaceholder": "국가 검색..."
    },
    "api": {
      "sectionTitle": "API 레퍼런스",
      "headers": {
        "prop": "Prop",
        "type": "타입",
        "default": "기본값",
        "description": "설명"
      },
      "descriptions": {
        "items": "필터링할 아이템 배열",
        "getItemLabel": "아이템에서 표시 텍스트를 추출",
        "onInputChange": "입력값 변경 시 호출 (비동기 검색용)",
        "onSelect": "아이템 선택 시 호출",
        "renderItem": "각 아이템의 커스텀 렌더링 함수",
        "placeholder": "입력 필드 placeholder 텍스트",
        "maxResults": "표시할 최대 결과 수",
        "debounceMs": "디바운스 지연 시간 (밀리초)",
        "noResultsMessage": "매칭 결과 없을 때 표시할 메시지",
        "loading": "로딩 인디케이터 표시",
        "disabled": "입력 비활성화"
      }
    },
    "a11y": {
      "sectionTitle": "접근성",
      "description": "WAI-ARIA Combobox 패턴을 따릅니다. WCAG 2.1 AA 준수.",
      "keyboard": "키보드 내비게이션",
      "ariaAttributes": "ARIA 속성",
      "keys": {
        "down": "다음 옵션으로 이동",
        "up": "이전 옵션으로 이동",
        "enter": "하이라이트된 옵션 선택",
        "escape": "목록 닫기"
      },
      "aria": {
        "combobox": "입력을 combobox로 식별",
        "expanded": "목록 열림 여부 표시",
        "activedescendant": "현재 하이라이트된 옵션을 가리킴",
        "listbox": "제안 목록을 식별",
        "option": "각 제안 항목을 식별",
        "selected": "하이라이트된 옵션을 표시"
      }
    }
  }
}
```

- [ ] **Step 4: Create `src/locales/en/autocomplete.json`**

```json
{
  "component": {
    "noResults": "No results",
    "loading": "Loading...",
    "resultCount_one": "{{count}} result",
    "resultCount_other": "{{count}} results"
  },
  "page": {
    "title": "Autocomplete",
    "description": "A combobox component that filters and suggests items as the user types. Supports keyboard navigation, custom rendering, and async data fetching.",
    "demo": {
      "sectionTitle": "Live Demo",
      "basic": "Basic — local filtering",
      "async": "Async — simulated API call (500ms delay)",
      "custom": "Custom rendering — countries with flags",
      "basicPlaceholder": "Search fruits...",
      "asyncPlaceholder": "Type to search fruits (async)...",
      "customPlaceholder": "Search countries..."
    },
    "api": {
      "sectionTitle": "API Reference",
      "headers": {
        "prop": "Prop",
        "type": "Type",
        "default": "Default",
        "description": "Description"
      },
      "descriptions": {
        "items": "Array of items to filter",
        "getItemLabel": "Extracts display text from item",
        "onInputChange": "Called when input value changes (for async search)",
        "onSelect": "Called when an item is selected",
        "renderItem": "Custom render function for each item",
        "placeholder": "Input placeholder text",
        "maxResults": "Maximum number of displayed results",
        "debounceMs": "Debounce delay in milliseconds",
        "noResultsMessage": "Message shown when no items match",
        "loading": "Shows loading indicator",
        "disabled": "Disables the input"
      }
    },
    "a11y": {
      "sectionTitle": "Accessibility",
      "description": "Follows the WAI-ARIA Combobox pattern. WCAG 2.1 AA compliant.",
      "keyboard": "Keyboard Navigation",
      "ariaAttributes": "ARIA Attributes",
      "keys": {
        "down": "Move to next option",
        "up": "Move to previous option",
        "enter": "Select highlighted option",
        "escape": "Close the listbox"
      },
      "aria": {
        "combobox": "Identifies the input as a combobox",
        "expanded": "Indicates whether the listbox is open",
        "activedescendant": "Points to the currently highlighted option",
        "listbox": "Identifies the suggestion list",
        "option": "Identifies each suggestion",
        "selected": "Indicates the highlighted option"
      }
    }
  }
}
```

- [ ] **Step 5: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/locales && git commit -m "feat: add ko/en translation JSON files for common and autocomplete"
```

---

### Task 3: Create i18n Initialization and Import

**Files:**
- Create: `apps/ui-kit/src/i18n.ts`
- Modify: `apps/ui-kit/src/main.tsx:1`

- [ ] **Step 1: Create `src/i18n.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

import koCommon from '@/locales/ko/common.json';
import koAutocomplete from '@/locales/ko/autocomplete.json';
import enCommon from '@/locales/en/common.json';
import enAutocomplete from '@/locales/en/autocomplete.json';

i18n.use(initReactI18next).init({
  resources: {
    ko: {
      common: koCommon,
      autocomplete: koAutocomplete,
    },
    en: {
      common: enCommon,
      autocomplete: enAutocomplete,
    },
  },
  lng: 'ko',
  fallbackLng: 'ko',
  ns: ['common', 'autocomplete'],
  defaultNs: 'common',
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

- [ ] **Step 2: Import i18n in `main.tsx`**

Add `import './i18n';` as the first import in `src/main.tsx`, before React imports:

```tsx
import './i18n';
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './global.css';
import { App } from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 3: Verify build**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm build
```

Expected: Build succeeds without errors.

- [ ] **Step 4: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/i18n.ts apps/ui-kit/src/main.tsx && git commit -m "feat: initialize i18next with ko/en resources"
```

---

### Task 4: Create LocaleLayout and Update Routing

**Files:**
- Create: `apps/ui-kit/src/layouts/LocaleLayout.tsx`
- Modify: `apps/ui-kit/src/App.tsx`

- [ ] **Step 1: Create `src/layouts/LocaleLayout.tsx`**

`LocaleLayout`은 URL의 `:locale` 파라미터를 읽어 `i18n.changeLanguage()`를 호출하고, 유효하지 않은 locale이면 `/ko/...`로 리다이렉트한다. 기존 `DocsLayout`을 자식으로 렌더링한다.

```tsx
import { useEffect } from 'react';
import { useParams, Navigate, Outlet } from 'react-router';
import { useTranslation } from 'react-i18next';

const SUPPORTED_LOCALES = ['ko', 'en'] as const;
type SupportedLocale = (typeof SUPPORTED_LOCALES)[number];

function isSupportedLocale(locale: string): locale is SupportedLocale {
  return SUPPORTED_LOCALES.includes(locale as SupportedLocale);
}

export function LocaleLayout() {
  const { locale } = useParams<{ locale: string }>();
  const { i18n } = useTranslation();

  useEffect(() => {
    if (locale && isSupportedLocale(locale) && i18n.language !== locale) {
      i18n.changeLanguage(locale);
    }
  }, [locale, i18n]);

  if (!locale || !isSupportedLocale(locale)) {
    return <Navigate to="/ko/components/autocomplete" replace />;
  }

  return <Outlet />;
}
```

- [ ] **Step 2: Update `src/App.tsx`**

라우트 구조를 `/:locale/components` 패턴으로 변경한다. `LocaleLayout`이 `DocsLayout`을 감싼다.

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router';
import { LocaleLayout } from '@/layouts/LocaleLayout';
import { DocsLayout } from '@/layouts/DocsLayout';
import { componentRoutes } from '@/routes';

export function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Navigate to="/ko/components/autocomplete" replace />} />
        <Route path="/:locale/components" element={<LocaleLayout />}>
          <Route element={<DocsLayout />}>
            {componentRoutes.map((route) => (
              <Route key={route.path} path={route.path} element={<route.component />} />
            ))}
          </Route>
        </Route>
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm build
```

Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/layouts/LocaleLayout.tsx apps/ui-kit/src/App.tsx && git commit -m "feat: add LocaleLayout and locale-based routing"
```

---

### Task 5: Update DocsLayout with Locale-Aware Links and Language Toggle

**Files:**
- Modify: `apps/ui-kit/src/layouts/DocsLayout.tsx`
- Modify: `apps/ui-kit/src/layouts/DocsLayout.module.css`

- [ ] **Step 1: Update `DocsLayout.tsx`**

NavLink의 경로를 현재 locale 기반으로 변경하고, 사이드바 상단에 `KO | EN` 언어 전환 링크를 추가한다.

```tsx
import { Suspense } from 'react';
import { NavLink, Outlet, useParams, Link, useLocation } from 'react-router';
import { useTranslation } from 'react-i18next';
import { componentRoutes } from '@/routes';
import styles from './DocsLayout.module.css';

export function DocsLayout() {
  const { locale } = useParams<{ locale: string }>();
  const { t } = useTranslation();
  const location = useLocation();

  function getLocaleTogglePath(targetLocale: string) {
    return location.pathname.replace(`/${locale}/`, `/${targetLocale}/`);
  }

  return (
    <div className={styles.layout}>
      <aside className={styles.sidebar}>
        <h1 className={styles.sidebarTitle}>{t('siteTitle')}</h1>
        <div className={styles.localeSwitcher}>
          <Link
            to={getLocaleTogglePath('ko')}
            className={`${styles.localeLink} ${locale === 'ko' ? styles.localeLinkActive : ''}`}
          >
            KO
          </Link>
          <span className={styles.localeDivider}>|</span>
          <Link
            to={getLocaleTogglePath('en')}
            className={`${styles.localeLink} ${locale === 'en' ? styles.localeLinkActive : ''}`}
          >
            EN
          </Link>
        </div>
        <nav className={styles.nav}>
          {componentRoutes.map((route) => (
            <NavLink
              key={route.path}
              to={`/${locale}/components/${route.path}`}
              className={({ isActive }) =>
                `${styles.navLink} ${isActive ? styles.navLinkActive : ''}`
              }
            >
              {route.label}
            </NavLink>
          ))}
        </nav>
      </aside>
      <main className={styles.main}>
        <Suspense fallback={<div>{t('loading')}</div>}>
          <Outlet />
        </Suspense>
      </main>
    </div>
  );
}
```

- [ ] **Step 2: Add language toggle styles to `DocsLayout.module.css`**

기존 CSS 끝에 추가:

```css
.localeSwitcher {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 16px;
  font-size: 13px;
}

.localeLink {
  text-decoration: none;
  color: #6b7280;
  font-weight: 400;
}

.localeLink:hover {
  color: #1a56db;
}

.localeLinkActive {
  color: #1a56db;
  font-weight: 700;
}

.localeDivider {
  color: #d1d5db;
}
```

- [ ] **Step 3: Verify build**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm build
```

Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/layouts/DocsLayout.tsx apps/ui-kit/src/layouts/DocsLayout.module.css && git commit -m "feat: add locale-aware NavLinks and language toggle to DocsLayout"
```

---

### Task 6: Integrate i18n into Autocomplete Component

**Files:**
- Modify: `apps/ui-kit/src/components/autocomplete/Autocomplete.tsx:29,91,119-121`
- Modify: `apps/ui-kit/src/components/autocomplete/Autocomplete.test.tsx`
- Modify: `apps/ui-kit/src/setup-tests.ts`

- [ ] **Step 1: Set up i18n mock for tests**

`setup-tests.ts`에 테스트용 i18n 초기화를 추가한다. 테스트에서는 `en` locale을 기본으로 사용하여 하드코딩 문자열과의 혼동을 방지한다.

```ts
import '@testing-library/jest-dom/vitest';
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

import enCommon from '@/locales/en/common.json';
import enAutocomplete from '@/locales/en/autocomplete.json';
import koCommon from '@/locales/ko/common.json';
import koAutocomplete from '@/locales/ko/autocomplete.json';

i18n.use(initReactI18next).init({
  resources: {
    en: { common: enCommon, autocomplete: enAutocomplete },
    ko: { common: koCommon, autocomplete: koAutocomplete },
  },
  lng: 'en',
  fallbackLng: 'en',
  ns: ['common', 'autocomplete'],
  defaultNs: 'common',
  interpolation: { escapeValue: false },
});
```

- [ ] **Step 2: Write failing test for i18n default text**

`Autocomplete.test.tsx`에 locale별 기본 텍스트 테스트를 추가한다.

```ts
import i18n from 'i18next';

// ... (기존 테스트 하단에 추가)

test('shows translated noResults message in English', async () => {
  await i18n.changeLanguage('en');
  const { user } = setup();
  const input = screen.getByRole('combobox');

  await user.type(input, 'xyz');
  await waitFor(() => {
    expect(screen.getByText('No results')).toBeInTheDocument();
  });
});

test('shows translated noResults message in Korean', async () => {
  await i18n.changeLanguage('ko');
  const { user } = setup();
  const input = screen.getByRole('combobox');

  await user.type(input, 'xyz');
  await waitFor(() => {
    expect(screen.getByText('결과 없음')).toBeInTheDocument();
  });
});

test('shows translated loading text', async () => {
  await i18n.changeLanguage('en');
  const { user } = setup({ loading: true });
  const input = screen.getByRole('combobox');

  await user.type(input, 'ap');
  await waitFor(() => {
    expect(screen.getByText('Loading...')).toBeInTheDocument();
  });
});

test('props override i18n default for noResultsMessage', async () => {
  await i18n.changeLanguage('en');
  const { user } = setup({ noResultsMessage: 'Custom empty' });
  const input = screen.getByRole('combobox');

  await user.type(input, 'xyz');
  await waitFor(() => {
    expect(screen.getByText('Custom empty')).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Run tests to verify they fail**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm test
```

Expected: New i18n tests fail because `Autocomplete.tsx` still uses hardcoded strings. Existing tests may also fail due to i18n setup changes — the `noResultsMessage` default is now `'결과 없음'` but the test expects `i18n('en')` to produce `'No results'`.

- [ ] **Step 4: Update `Autocomplete.tsx` to use i18n**

세 곳을 변경한다:
1. `noResultsMessage` 기본값: `'결과 없음'` → `t('component.noResults')`
2. Loading 텍스트: `'Loading...'` → `t('component.loading')`
3. Screen reader 텍스트: `` `${filteredItems.length}개의 결과` `` → `t('component.resultCount', { count: filteredItems.length })`

```tsx
import { useRef, useId, type ReactNode } from 'react';
import { useTranslation } from 'react-i18next';
import { useAutocomplete } from './useAutocomplete';
import { useClickOutside } from '@/hooks/useClickOutside';
import styles from './Autocomplete.module.css';

export interface AutocompleteProps<T> {
  items: T[];
  getItemLabel: (item: T) => string;
  onInputChange?: (value: string) => void;
  onSelect?: (item: T) => void;
  renderItem?: (item: T, highlighted: boolean) => ReactNode;
  placeholder?: string;
  maxResults?: number;
  debounceMs?: number;
  noResultsMessage?: string;
  loading?: boolean;
  disabled?: boolean;
}

export function Autocomplete<T>({
  items,
  getItemLabel,
  onInputChange,
  onSelect,
  renderItem,
  placeholder,
  maxResults = 10,
  debounceMs = 300,
  noResultsMessage,
  loading = false,
  disabled = false,
}: AutocompleteProps<T>) {
  const { t } = useTranslation('autocomplete');
  const id = useId();
  const listboxId = `${id}-listbox`;
  const containerRef = useRef<HTMLDivElement>(null);
  const inputRef = useRef<HTMLInputElement>(null);

  const {
    inputValue,
    isOpen,
    highlightedIndex,
    filteredItems,
    handleInputChange,
    handleKeyDown,
    selectItem,
    close,
  } = useAutocomplete({
    items,
    getItemLabel,
    onInputChange,
    onSelect,
    maxResults,
    debounceMs,
  });

  useClickOutside(containerRef, close);

  const showListbox = isOpen && !disabled;
  const showNoResults = showListbox && !loading && filteredItems.length === 0 && inputValue.length > 0;
  const resolvedNoResults = noResultsMessage ?? t('component.noResults');

  function getOptionId(index: number) {
    return `${id}-option-${index}`;
  }

  return (
    <div ref={containerRef} className={styles.container}>
      <input
        ref={inputRef}
        role="combobox"
        aria-expanded={showListbox}
        aria-haspopup="listbox"
        aria-autocomplete="list"
        aria-controls={showListbox ? listboxId : undefined}
        aria-activedescendant={
          showListbox && highlightedIndex >= 0
            ? getOptionId(highlightedIndex)
            : undefined
        }
        className={styles.input}
        value={inputValue}
        placeholder={placeholder}
        disabled={disabled}
        onChange={(e) => handleInputChange(e.target.value)}
        onKeyDown={handleKeyDown}
      />

      {showListbox && (
        <ul id={listboxId} role="listbox" className={styles.listbox}>
          {loading ? (
            <li role="status" className={styles.loading}>
              {t('component.loading')}
            </li>
          ) : showNoResults ? (
            <li className={styles.noResults}>{resolvedNoResults}</li>
          ) : (
            filteredItems.map((item, index) => (
              <li
                key={getItemLabel(item)}
                id={getOptionId(index)}
                role="option"
                aria-selected={index === highlightedIndex}
                className={`${styles.option} ${index === highlightedIndex ? styles.optionHighlighted : ''}`}
                onMouseDown={(e) => {
                  e.preventDefault();
                  selectItem(item);
                  inputRef.current?.focus();
                }}
              >
                {renderItem
                  ? renderItem(item, index === highlightedIndex)
                  : getItemLabel(item)}
              </li>
            ))
          )}
        </ul>
      )}

      <div role="log" aria-live="polite" className={styles.srOnly}>
        {showListbox && !loading && filteredItems.length > 0
          ? t('component.resultCount', { count: filteredItems.length })
          : ''}
      </div>
    </div>
  );
}
```

Key changes from original:
- Added `import { useTranslation } from 'react-i18next'`
- `noResultsMessage` default changed from `'결과 없음'` to `undefined`; resolved via `noResultsMessage ?? t('component.noResults')`
- `Loading...` → `{t('component.loading')}`
- `` `${filteredItems.length}개의 결과` `` → `t('component.resultCount', { count: filteredItems.length })`

- [ ] **Step 5: Update existing test for noResultsMessage**

기존 `shows noResultsMessage when no matches` 테스트는 `noResultsMessage: 'No results'`를 props로 전달하므로 변경 불필요하다. 단, 기존 `announces result count to screen readers` 테스트는 `'2'`만 확인하는데, 영문 locale에서는 `'2 results'`가 출력되므로 수정한다.

```ts
test('announces result count to screen readers', async () => {
  await i18n.changeLanguage('en');
  const { user } = setup();
  const input = screen.getByRole('combobox');

  await user.type(input, 'ap');

  await waitFor(() => {
    const liveRegion = screen.getByRole('log');
    expect(liveRegion).toHaveTextContent('2 results');
  });
});
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm test
```

Expected: All tests pass, including the new i18n tests.

- [ ] **Step 7: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/components/autocomplete/Autocomplete.tsx apps/ui-kit/src/components/autocomplete/Autocomplete.test.tsx apps/ui-kit/src/setup-tests.ts && git commit -m "feat: integrate i18n into Autocomplete component with locale-aware defaults"
```

---

### Task 7: Integrate i18n into AutocompletePage

**Files:**
- Modify: `apps/ui-kit/src/pages/autocomplete/AutocompletePage.tsx`

- [ ] **Step 1: Replace all hardcoded text with `t()` calls**

```tsx
import { useState, useCallback, useRef } from 'react';
import { useTranslation } from 'react-i18next';
import { Autocomplete } from '@/components/autocomplete';
import styles from './AutocompletePage.module.css';

const fruits = [
  'Apple', 'Apricot', 'Banana', 'Blueberry', 'Cherry',
  'Cranberry', 'Grape', 'Kiwi', 'Lemon', 'Mango',
  'Orange', 'Peach', 'Pear', 'Pineapple', 'Strawberry',
];

interface Country {
  name: string;
  emoji: string;
}

const countries: Country[] = [
  { name: 'South Korea', emoji: '\uD83C\uDDF0\uD83C\uDDF7' },
  { name: 'Japan', emoji: '\uD83C\uDDEF\uD83C\uDDF5' },
  { name: 'United States', emoji: '\uD83C\uDDFA\uD83C\uDDF8' },
  { name: 'United Kingdom', emoji: '\uD83C\uDDEC\uD83C\uDDE7' },
  { name: 'Germany', emoji: '\uD83C\uDDE9\uD83C\uDDEA' },
  { name: 'France', emoji: '\uD83C\uDDEB\uD83C\uDDF7' },
  { name: 'Canada', emoji: '\uD83C\uDDE8\uD83C\uDDE6' },
  { name: 'Australia', emoji: '\uD83C\uDDE6\uD83C\uDDFA' },
];

function AsyncDemo() {
  const { t } = useTranslation('autocomplete');
  const [items, setItems] = useState<string[]>([]);
  const [loading, setLoading] = useState(false);
  const timerRef = useRef<ReturnType<typeof setTimeout>>(null);

  const handleInputChange = useCallback((value: string) => {
    if (timerRef.current) clearTimeout(timerRef.current);
    if (!value) {
      setItems([]);
      setLoading(false);
      return;
    }
    setLoading(true);
    timerRef.current = setTimeout(() => {
      const query = value.toLowerCase();
      setItems(fruits.filter((f) => f.toLowerCase().startsWith(query)));
      setLoading(false);
    }, 500);
  }, []);

  return (
    <Autocomplete
      items={items}
      getItemLabel={(item) => item}
      onInputChange={handleInputChange}
      loading={loading}
      placeholder={t('page.demo.asyncPlaceholder')}
      debounceMs={300}
    />
  );
}

export default function AutocompletePage() {
  const { t } = useTranslation('autocomplete');

  return (
    <div className={styles.page}>
      <header>
        <h2 className={styles.title}>{t('page.title')}</h2>
        <p className={styles.description}>{t('page.description')}</p>
      </header>

      <section className={styles.section}>
        <h3 className={styles.sectionTitle}>{t('page.demo.sectionTitle')}</h3>

        <div className={styles.demo}>
          <p className={styles.demoLabel}>{t('page.demo.basic')}</p>
          <Autocomplete
            items={fruits}
            getItemLabel={(item) => item}
            placeholder={t('page.demo.basicPlaceholder')}
            debounceMs={0}
          />
        </div>

        <div className={styles.demo}>
          <p className={styles.demoLabel}>{t('page.demo.async')}</p>
          <AsyncDemo />
        </div>

        <div className={styles.demo}>
          <p className={styles.demoLabel}>{t('page.demo.custom')}</p>
          <Autocomplete
            items={countries}
            getItemLabel={(item) => item.name}
            placeholder={t('page.demo.customPlaceholder')}
            debounceMs={0}
            renderItem={(item, highlighted) => (
              <div
                className={styles.richItem}
                style={{ fontWeight: highlighted ? 600 : 400 }}
              >
                <span className={styles.richItemEmoji}>{item.emoji}</span>
                <span>{item.name}</span>
              </div>
            )}
          />
        </div>
      </section>

      <section className={styles.section}>
        <h3 className={styles.sectionTitle}>{t('page.api.sectionTitle')}</h3>
        <table className={styles.table}>
          <thead>
            <tr>
              <th>{t('page.api.headers.prop')}</th>
              <th>{t('page.api.headers.type')}</th>
              <th>{t('page.api.headers.default')}</th>
              <th>{t('page.api.headers.description')}</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><code>items</code></td>
              <td><code>T[]</code></td>
              <td>required</td>
              <td>{t('page.api.descriptions.items')}</td>
            </tr>
            <tr>
              <td><code>getItemLabel</code></td>
              <td><code>{'(item: T) => string'}</code></td>
              <td>required</td>
              <td>{t('page.api.descriptions.getItemLabel')}</td>
            </tr>
            <tr>
              <td><code>onInputChange</code></td>
              <td><code>{'(value: string) => void'}</code></td>
              <td>-</td>
              <td>{t('page.api.descriptions.onInputChange')}</td>
            </tr>
            <tr>
              <td><code>onSelect</code></td>
              <td><code>{'(item: T) => void'}</code></td>
              <td>-</td>
              <td>{t('page.api.descriptions.onSelect')}</td>
            </tr>
            <tr>
              <td><code>renderItem</code></td>
              <td><code>{'(item: T, highlighted: boolean) => ReactNode'}</code></td>
              <td>-</td>
              <td>{t('page.api.descriptions.renderItem')}</td>
            </tr>
            <tr>
              <td><code>placeholder</code></td>
              <td><code>string</code></td>
              <td>-</td>
              <td>{t('page.api.descriptions.placeholder')}</td>
            </tr>
            <tr>
              <td><code>maxResults</code></td>
              <td><code>number</code></td>
              <td><code>10</code></td>
              <td>{t('page.api.descriptions.maxResults')}</td>
            </tr>
            <tr>
              <td><code>debounceMs</code></td>
              <td><code>number</code></td>
              <td><code>300</code></td>
              <td>{t('page.api.descriptions.debounceMs')}</td>
            </tr>
            <tr>
              <td><code>noResultsMessage</code></td>
              <td><code>string</code></td>
              <td><code>{`"${t('component.noResults')}"`}</code></td>
              <td>{t('page.api.descriptions.noResultsMessage')}</td>
            </tr>
            <tr>
              <td><code>loading</code></td>
              <td><code>boolean</code></td>
              <td><code>false</code></td>
              <td>{t('page.api.descriptions.loading')}</td>
            </tr>
            <tr>
              <td><code>disabled</code></td>
              <td><code>boolean</code></td>
              <td><code>false</code></td>
              <td>{t('page.api.descriptions.disabled')}</td>
            </tr>
          </tbody>
        </table>
      </section>

      <section className={styles.section}>
        <h3 className={styles.sectionTitle}>{t('page.a11y.sectionTitle')}</h3>
        <p>
          {t('page.a11y.description')}
        </p>
        <h4>{t('page.a11y.keyboard')}</h4>
        <ul className={styles.kbdList}>
          <li className={styles.kbdItem}>
            <kbd className={styles.kbd}>↓</kbd> <span>{t('page.a11y.keys.down')}</span>
          </li>
          <li className={styles.kbdItem}>
            <kbd className={styles.kbd}>↑</kbd> <span>{t('page.a11y.keys.up')}</span>
          </li>
          <li className={styles.kbdItem}>
            <kbd className={styles.kbd}>Enter</kbd> <span>{t('page.a11y.keys.enter')}</span>
          </li>
          <li className={styles.kbdItem}>
            <kbd className={styles.kbd}>Esc</kbd> <span>{t('page.a11y.keys.escape')}</span>
          </li>
        </ul>
        <h4>{t('page.a11y.ariaAttributes')}</h4>
        <table className={styles.table}>
          <thead>
            <tr>
              <th>Attribute</th>
              <th>Element</th>
              <th>Usage</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><code>role="combobox"</code></td>
              <td>Input</td>
              <td>{t('page.a11y.aria.combobox')}</td>
            </tr>
            <tr>
              <td><code>aria-expanded</code></td>
              <td>Input</td>
              <td>{t('page.a11y.aria.expanded')}</td>
            </tr>
            <tr>
              <td><code>aria-activedescendant</code></td>
              <td>Input</td>
              <td>{t('page.a11y.aria.activedescendant')}</td>
            </tr>
            <tr>
              <td><code>role="listbox"</code></td>
              <td>List</td>
              <td>{t('page.a11y.aria.listbox')}</td>
            </tr>
            <tr>
              <td><code>role="option"</code></td>
              <td>List item</td>
              <td>{t('page.a11y.aria.option')}</td>
            </tr>
            <tr>
              <td><code>aria-selected</code></td>
              <td>List item</td>
              <td>{t('page.a11y.aria.selected')}</td>
            </tr>
          </tbody>
        </table>
      </section>
    </div>
  );
}
```

- [ ] **Step 2: Verify build**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm build
```

Expected: Build succeeds.

- [ ] **Step 3: Run unit tests**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm test
```

Expected: All tests pass.

- [ ] **Step 4: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/src/pages/autocomplete/AutocompletePage.tsx && git commit -m "feat: replace hardcoded text with i18n t() calls in AutocompletePage"
```

---

### Task 8: Update E2E Tests for Locale-Based URLs

**Files:**
- Modify: `apps/ui-kit/e2e/autocomplete.spec.ts`

- [ ] **Step 1: Update E2E tests**

URL을 `/:locale/` 기반으로 변경하고, 양쪽 locale에서의 테스트와 언어 전환 테스트를 추가한다.

```ts
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

test.describe('Autocomplete (ko)', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/ko/components/autocomplete');
  });

  test('page loads with Korean text', async ({ page }) => {
    await expect(page.getByRole('heading', { name: 'Autocomplete' })).toBeVisible();
    await expect(page.getByPlaceholder('과일 검색...')).toBeVisible();
  });

  test('basic demo: type and select with mouse', async ({ page }) => {
    const input = page.getByPlaceholder('과일 검색...');
    await input.fill('ap');

    const listbox = page.getByRole('listbox').first();
    await expect(listbox).toBeVisible();

    const option = page.getByRole('option', { name: 'Apple' });
    await expect(option).toBeVisible();
    await option.click();

    await expect(input).toHaveValue('Apple');
    await expect(listbox).not.toBeVisible();
  });

  test('keyboard-only flow: navigate and select', async ({ page }) => {
    const input = page.getByPlaceholder('과일 검색...');
    await input.fill('b');

    await expect(page.getByRole('listbox').first()).toBeVisible();

    await input.press('ArrowDown');
    await input.press('Enter');

    await expect(input).toHaveValue('Banana');
    await expect(page.getByRole('listbox').first()).not.toBeVisible();
  });

  test('Escape closes the listbox', async ({ page }) => {
    const input = page.getByPlaceholder('과일 검색...');
    await input.fill('ch');

    await expect(page.getByRole('listbox').first()).toBeVisible();

    await input.press('Escape');
    await expect(page.getByRole('listbox').first()).not.toBeVisible();
  });

  test('axe accessibility scan passes', async ({ page }) => {
    const input = page.getByPlaceholder('과일 검색...');
    await input.fill('ap');
    await expect(page.getByRole('listbox').first()).toBeVisible();

    const results = await new AxeBuilder({ page }).analyze();
    expect(results.violations).toEqual([]);
  });
});

test.describe('Autocomplete (en)', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/en/components/autocomplete');
  });

  test('page loads with English text', async ({ page }) => {
    await expect(page.getByRole('heading', { name: 'Autocomplete' })).toBeVisible();
    await expect(page.getByPlaceholder('Search fruits...')).toBeVisible();
  });

  test('basic demo: type and select', async ({ page }) => {
    const input = page.getByPlaceholder('Search fruits...');
    await input.fill('ap');

    const listbox = page.getByRole('listbox').first();
    await expect(listbox).toBeVisible();

    const option = page.getByRole('option', { name: 'Apple' });
    await expect(option).toBeVisible();
    await option.click();

    await expect(input).toHaveValue('Apple');
    await expect(listbox).not.toBeVisible();
  });

  test('axe accessibility scan passes', async ({ page }) => {
    const input = page.getByPlaceholder('Search fruits...');
    await input.fill('ap');
    await expect(page.getByRole('listbox').first()).toBeVisible();

    const results = await new AxeBuilder({ page }).analyze();
    expect(results.violations).toEqual([]);
  });
});

test.describe('Language switching', () => {
  test('switches from ko to en via toggle', async ({ page }) => {
    await page.goto('/ko/components/autocomplete');
    await expect(page.getByPlaceholder('과일 검색...')).toBeVisible();

    await page.getByRole('link', { name: 'EN' }).click();

    await expect(page).toHaveURL(/\/en\/components\/autocomplete/);
    await expect(page.getByPlaceholder('Search fruits...')).toBeVisible();
  });

  test('switches from en to ko via toggle', async ({ page }) => {
    await page.goto('/en/components/autocomplete');
    await expect(page.getByPlaceholder('Search fruits...')).toBeVisible();

    await page.getByRole('link', { name: 'KO' }).click();

    await expect(page).toHaveURL(/\/ko\/components\/autocomplete/);
    await expect(page.getByPlaceholder('과일 검색...')).toBeVisible();
  });

  test('root redirects to /ko/components/autocomplete', async ({ page }) => {
    await page.goto('/');
    await expect(page).toHaveURL(/\/ko\/components\/autocomplete/);
  });
});
```

- [ ] **Step 2: Run E2E tests**

```bash
cd /Users/taepyeong/Workspace/projects/showcase/apps/ui-kit && pnpm test:e2e
```

Expected: All E2E tests pass.

- [ ] **Step 3: Commit**

```bash
cd /Users/taepyeong/Workspace/projects/showcase && git add apps/ui-kit/e2e/autocomplete.spec.ts && git commit -m "feat: update E2E tests for locale-based URLs and add language switching tests"
```

---

Plan complete and saved to `docs/superpowers/plans/2026-04-08-i18n.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?